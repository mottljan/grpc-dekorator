pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
    }
}

rootProject.name = "grpc-dekorator"

include(":dekorator-api")
include(":dekorator-processor")
include(":testing")
